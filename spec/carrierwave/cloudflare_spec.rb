RSpec.describe Carrierwave::Cloudflare do
  it "has a version number" do
    expect(Carrierwave::Cloudflare::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
